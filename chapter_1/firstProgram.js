//Code run using only Javascript alerts

// alert("Hello, world!");
// alert("What is your name?");
// var myName = prompt();
// alert("It is good to meet you, " + myName);
// alert("The length of your name is:");
// alert(myName.length);
// alert("What is your age?");
// var myAge = prompt();
// alert("You will be " + (parseInt(myAge) + 1) + " in a year.");

//Code run in conjunction with HTML and CSS

document.addEventListener("keydown", function(event){
    // console.log(event.key);
    enter(event.key);
});

function enter(key){
    switch (key) {
        case "Enter":
            var myName = 
    }
}
